import { NamedFeatherIconProps } from "@/types/feather";
import FeatherIcon from "feather-icons-react";

export default function Icon(props: NamedFeatherIconProps) {
    return <FeatherIcon {...props} />;
}
